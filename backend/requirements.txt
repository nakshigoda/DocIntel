# DocIntel AI — Backend Dependencies
# Direct dependencies only. Pin to the versions confirmed working in the venv.
# Run: pip install -r requirements.txt

# --- Web framework ---
fastapi==0.136.3
uvicorn[standard]==0.49.0
python-multipart==0.0.32   # required for UploadFile / Form

# --- Data validation ---
pydantic==2.13.4

# --- Database ---
pymongo==4.17.0
python-dotenv==1.2.2

# --- PDF processing ---
PyMuPDF==1.27.2.3

# --- Embeddings (local, no API key) ---
sentence-transformers==5.5.1

# --- Vector database ---
chromadb==1.5.9

# --- LLM (Groq cloud API) ---
groq==1.4.0

# --- Numerical ---
numpy==2.4.6

# --- FAISS (retained for backward compat, will be removed in Phase 5) ---
faiss-cpu==1.14.3
